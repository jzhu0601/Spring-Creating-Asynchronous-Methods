# Spring-Creating-Asynchronous-Methods

A lookup service that queries GitHub user information and retrieves data through GitHub’s API.

An asynchronous service that lets you scale multiple calls at once.

         
    ./gradlew build
    
    
